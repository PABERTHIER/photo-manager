name: Build & Test

on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 8.0.x

      - name: Display .NET version
        run: dotnet --info

      - name: Restore dependencies
        run: dotnet restore PhotoManager/PhotoManager.sln

      - name: Build
        run: dotnet build --no-restore --configuration Release PhotoManager/PhotoManager.sln

      - name: Test
        run: dotnet test --no-build --configuration Release --verbosity normal PhotoManager/PhotoManager.Tests/PhotoManager.Tests.csproj

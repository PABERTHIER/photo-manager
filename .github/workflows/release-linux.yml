name: Release - Linux

on:
  workflow_dispatch: # Manual trigger for a release
  push:
    tags:
      - "v*" # Trigger on tags like v1.0.0

permissions:
  contents: write

jobs:
  release-linux:
    name: Linux Release
    runs-on: ubuntu-latest

    env:
      PROJECT_NAME: PhotoManager/PhotoManager.slnx
      DOTNET_VERSION: 10.0.x

    steps:
      # Step 1: Checkout the code
      - name: ⤵️ Checkout Source
        uses: actions/checkout@v6
        with:
          fetch-depth: 0 # Ensures full history is fetched

      # Step 2: Setup .NET SDK
      - name: 🛠️ Setup .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      # Step 3: Cache NuGet packages
      - name: 💾 Cache NuGet packages
        uses: actions/cache@v5
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj', '**/Directory.Packages.props') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      # Step 4: Restore dependencies
      - name: 🛠️ Restore dependencies
        run: dotnet restore ${{ env.PROJECT_NAME }}

      # Step 5: Build the project for release
      - name: 🔨 Build
        run: dotnet build --no-restore --configuration Release ${{ env.PROJECT_NAME }}

      # Step 6: Publish the project
      - name: 📦 Publish
        run: >-
          dotnet publish --configuration Release --runtime linux-x64 --output ./output/linux-x64
          --self-contained true -p:UseAppHost=true PhotoManager/PhotoManager.UI/PhotoManager.UI.csproj

      # Step 7: Add Linux desktop integration files
      - name: 📂 Add Desktop Integration Files
        run: |
          test -f ./output/linux-x64/PhotoManager.UI
          chmod +x ./output/linux-x64/PhotoManager.UI
          cp PhotoManager/PhotoManager.UI/Assets/PhotoManager.desktop ./output/linux-x64/
          cp PhotoManager/PhotoManager.UI/Assets/AppIcon.png ./output/linux-x64/photomanager.png
          test -x ./output/linux-x64/PhotoManager.UI
          test -f ./output/linux-x64/PhotoManager.desktop
          test -f ./output/linux-x64/photomanager.png

      # Step 8: Compress output into a tar.gz file
      - name: 📂 Create tar.gz Archive
        run: |
          cd ./output/linux-x64
          tar -czf ../../photo-manager-${{ github.ref_name }}-linux-x64.tar.gz .
          cd ../..

      # Step 9: Create Release with GitHub CLI
      - name: 🏷️ Create Release (if not exists)
        run: |
          gh release create ${{ github.ref_name }} --title "PhotoManager ${{ github.ref_name }}" \
            --generate-notes || true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Step 10: Wait for Release availability
      - name: ⏳ Wait for Release availability
        run: |
          for i in $(seq 1 10); do
            if gh release view ${{ github.ref_name }} > /dev/null 2>&1; then
              echo "Release found"
              exit 0
            fi
            echo "Waiting for release... attempt $i/10"
            sleep 5
          done
          echo "::error::Release ${{ github.ref_name }} not found after retries"
          exit 1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Step 11: Upload File as Artifact
      - name: 📤 Upload Release Asset
        run: |
          gh release upload ${{ github.ref_name }} ./photo-manager-${{ github.ref_name }}-linux-x64.tar.gz --clobber
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Step 12: Upload Build Artifacts
      - name: 💾 Upload Build Artifacts
        uses: actions/upload-artifact@v6
        with:
          name: photo-manager-linux-x64
          path: ./output/linux-x64

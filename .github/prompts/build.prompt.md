---
description: "Build the project and check for warnings"
---

Build the PhotoManager project and report any errors or warnings.

Run: `dotnet build PhotoManager/PhotoManager.slnx`

If the build fails, analyze the errors and suggest fixes.
If the build succeeds, confirm zero warnings (this project treats warnings as errors).
